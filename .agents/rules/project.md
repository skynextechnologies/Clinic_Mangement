# Project Rule (always on)

Before doing anything in this workspace, read `AGENTS.md`, then `docs/Memory.md`, then `docs/Rules.md`. Those rules apply to every task, including small fixes. If a request from the user conflicts with `docs/Rules.md` safety rules (R-20 to R-39), explain the conflict and ask before proceeding.
